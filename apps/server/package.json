{
    "name": "@stacklearn/server",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "bun run --watch src/index.ts",
        "build": "bun build src/index.ts --outdir dist --target bun",
        "start": "bun run dist/index.js"
    },
    "dependencies": {
        "hono": "^4.4.0"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "typescript": "^5.5.0"
    }
}